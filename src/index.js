import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

// connects to the root div located in the index.html, within the initialized public folder
ReactDOM.render(<App />, document.getElementById('root')); 